name = "wekit-read-receipts-cf-workers"
main = "src/index.js"
compatibility_date = "2026-08-01"

# D1 绑定：不填 database_id，首次部署时自动创建并绑定
# （Automatic provisioning，需 Wrangler ≥ 4.45.0；ID 仅存于 dashboard，不写回仓库）
[[d1_databases]]
binding = "DB"
database_name = "read-receipts"

# 定时清理过期会话及审计日志
[triggers]
crons = ["0 3 * * *"]
